flask==1.1.2   # An older version with known vulnerabilities
requests==2.25.0  # A version that has vulnerabilities
pyyaml==5.3.1  # Older version with security flaws
django==4.2.16  # An older Django version
numpy==1.18.5  # Older version of numpy
